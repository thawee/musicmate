<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#80000000"/> <!-- Semi-transparent black, for example -->
    <corners android:radius="4dp"/> <!-- Adjust radius as needed -->
    <stroke android:width="1dp" android:color="@color/your_highlight_border_color" /> <!-- Optional border -->
</shape>