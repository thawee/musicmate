package apincer.android.listener;

import androidx.annotation.NonNull;
import androidx.fragment.app.DialogFragment;

public interface OnDialogButtonClickListener {
    void onClick(@NonNull DialogFragment dialog);
}
